[project]
name = "irctc-pnr-notifier"
version = "0.1.0"
description = "Daily IRCTC PNR status checker with notifications"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
]

[tool.uv]
package = false
